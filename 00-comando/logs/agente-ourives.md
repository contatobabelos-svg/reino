| início | status | conclusão | o que fez |
|---|---|---|---|
| 2026-09-19 03:44 | em andamento | | Nova camada "Empresas da região" no ReinoMapa.jsx, lendo empresas_reais do Supabase |
| 2026-09-19 03:48 | concluído | 2026-09-19 03:56 | AcademyScreen.jsx passou a ler academy_trilhas/academy_aulas do Supabase (REST público), com formulário de admin que chama a Edge Function academy-importar (cola URL do YouTube, vídeo ou canal), aviso separado para o 402 da youtube138, player youtube-nocookie e seção "Aulas locais" preservando o localStorage antigo. checar-jsx.js passou; curl confirmou 200 nas duas tabelas (vazias); testado com Playwright headless em 1440x900 e 390x844, sem erros de console próprios da tela (só um 400 pré-existente do login demo tentando autenticar no Supabase Auth, já tratado pelo contas.js) |
| 2026-09-19 04:00 | concluído | 2026-09-19 04:17 | Pedido direto do fundador (portal de notícias): nova tela NoticiasScreen.jsx (rota noticias.html, menu "Notícias do Reino", ícone `noticias` já existia no design system) lendo manchetes via nova Edge Function supabase/functions/noticias-tempo-real (fontes Google News13 por categoria e News Briefs por id da RapidAPI), chave só no Vault via segredo_rapidapi() — a mesma do academy-importar — NUNCA no navegador; a função exige login (qualquer conta, não só admin) pra ler. **Achado crítico não relacionado ao pedido, corrigido**: `_ds_bundle.js` embute uma cópia velha de contas.js/fotos.js/afiliados.js que, carregada depois no index.html, apagava `window.ReinoContas.token` (e afins) inteiro — quebrando silenciosamente qualquer recurso que dependa do token do login de verdade (Contas no banco, import da Academy, e agora Notícias) mesmo com o usuário logado. Reordenei os `<script>` em ui_kits/babel-os/index.html pra afiliados.js/contas.js/fotos.js carregarem DEPOIS do _ds_bundle.js — comentário explicando o motivo ficou no próprio arquivo. Confirmei com Playwright que window.ReinoContas.token virou function de novo e que Dashboard/Academy/Admin continuam sem erro novo de console. **Não testei a Edge Function de notícias com dado real**: as duas chaves RapidAPI que o fundador colou no chat responderam "You are not subscribed to this API." pra google-news13 e pra news-briefs-read-only — a função e a tela tratam esse caso como aviso (402), mas ninguém assinou essas APIs ainda na RapidAPI. Não deployei a função (o `supabase` CLI deste ambiente só enxerga um projeto diferente do fxlansnepokjxdikxocb, sem permissão pro projeto do Reino) — fica pra quem tiver acesso ao projeto certo rodar `supabase functions deploy noticias-tempo-real`. Pedido de API do Spotify (spotify23) e do mapa (wft-geo-db) squeceram no meio da conversa: respondi ao fundador sem tocar em ReinoMapa.jsx, que é do outro Ourives. |
| 2026-09-19 03:44 | concluído | 2026-09-19 03:59 | Nova camada "Empresas da região" em ReinoMapa.jsx: busca em `empresas_reais` do Supabase (chave publicável, sem cache HTTP extra, cache em memória por UF), cor verde `#34e6a6`, cartão com foto/categoria/endereço/nota/telefone/site e linha fixa "Fonte: Google Maps · não é membro do Reino" (não entra em nenhum KPI/calor do Reino). checar-jsx.js passou; curl confirmou dados reais (SP com registros, 358 linhas no total); testado com harness Playwright headless (Chrome do sistema) em 1440x900 e 390x844 — chip "Região" visível e alternável, cluster verde renderiza, clique num ponto abre o cartão certo (ex.: "DTF São Paulo"), sem erros de console (só favicon.ico 404, irrelevante) |
| 2026-09-19 04:43 | concluído | 2026-09-19 05:14 | 4 recursos autorizados pelo fundador, todos sobre a Edge Function pronta `reino-apis` (funciona sem login): (1) NoticiasScreen.jsx reescrita do zero (a versão antiga usava a Edge Function `noticias-tempo-real`, que não existe mais) — pills Manchetes/Negócios/Empreendedorismo/Tecnologia/Mercado/Varejo (busco sem acento na API, rótulo com acento na tela), grade de cartões com "há Xh" calculado do `publicado`, link em nova aba com `rel="noopener noreferrer"`, botão Atualizar, estados carregando/erro/vazio, rodapé "Fonte: Google Notícias". (2) MusicaScreen.jsx nova — busca (padrão "samba" + chips de sugestão), lista com capa/título/artista, um único `<audio>` compartilhado por tela (toca uma faixa e pausa a anterior automaticamente), link "Deezer ↗", rodapé "Prévias de 30 s · Fonte: Deezer". (3) AssistenteScreen.jsx nova — chat com `.hg-chat`/`.hg-chat-msg` (classes já existiam no CSS, sem uso ainda), aviso fixo no topo sobre valores/comissão, chips de pergunta, Enter envia, quando a resposta trai `ir` mostra botão "Abrir <nome da tela>" (nome vem do `MENU_BABEL` do design system) que chama a prop `ir` recebida do App. (4) Globo: em `globo.js` adicionei `populacaoCidade(uf,cidade)` com cache em memória por "UF|cidade" (chama `reino-apis` rota "cidade"), disparada em `irCidade`; no nível "cidade" do `renderPainel` mostra "População: N (IBGE · Censo 2022)" formatada com o `num()` já injetado por opts (mesmo toLocaleString pt-BR pedido) — se falhar, `dados.populacao` fica `null` e a linha não aparece. As 3 telas novas entraram em `App.jsx` (TELAS + 3 itens de menu antes de "Minha conta", ícones `noticias`/`grafico`/`buscaIA`, já existentes no design system) e os 3 scripts em `index.html` antes de `PreCadastroScreen.jsx`. **Achado durante o teste do globo, não é bug meu**: `MapaPanel.jsx` troca automaticamente para o `ReinoMapa` 2D (OpenFreeMap) assim que `nivel` sai de "terra"/"lua" — então o `.hg-globo-painel` 3D (onde pus a população) fica com `visibility:hidden` por trás do mapa 2D na tela "Mapa Reino" normal; não toquei nessa troca (fora do pedido). Para a evidência visual da população, desliguei `window.ReinoMapa` só na sessão de teste (sem tocar no código) e confirmei visualmente "População: 1.139.047 (IBGE · Censo 2022)" no painel; sem essa desativação, o valor está certo no DOM (confirmado por innerHTML) mas escondido pela troca de mapa — decisão de mexer nisso é da Vyra/fundador. Corrigi de quebra um "CSS Grid blowout" em Música/Assistente (formulário com `<Input flex:1>` estourava a largura no mobile porque o grid pai (`.hg-rolar`/div grid) não tinha `gridTemplateColumns` — botão "Buscar"/"Enviar" ficava cortado em 390px); adicionei `gridTemplateColumns:"minmax(0,1fr)"` nos três wrappers em grid (Notícias, Música, Assistente). Verificação: `node .claude/dominic/checar-jsx.js` limpo nos 4 arquivos alterados/criados; `node --check globo.js` ok; `sh montar-site.sh` + Playwright headless (venv própria, Chromium baixado na hora) em 1440×900 e 390×844 — Notícias/Música/Assistente com dados reais da API (30 manchetes, 20 faixas do Deezer, resposta real do assistente com botão "Abrir Mapa Reino"), zero erros de console nas duas viewports; globo com população confirmada via `irEndereco({uf:'SP',cidade:'Campinas'})` chamado na API pública do componente (mesmo caminho que o clique no painel usa). Não verifiquei: toque real em mobile físico (só emulação Playwright) e o áudio realmente tocando som (confirmei só o estado play/pause e ausência de erro, não ouvi o som). |
| 2026-09-19 05:47 | concluído | 2026-09-19 06:16 | Pedido do fundador (cara de site de notícias + bloco no dashboard + assistente com rosto vivo em pixels): NoticiasScreen.jsx virou portal de jornal — cabeçalho com data de hoje por extenso em pt-BR e "Última atualização hh:mm", editorias em abas sublinhadas, manchete principal grande com kicker (fonte em caixa alta), coluna lateral "Mais lidas" numerada, grade de cartões abaixo, links sempre em nova aba, sem foto inventada (tipografia, filetes e faixa ciano do padrão); CSS novo em app-shell.css (.hg-jornal-*), responsivo (1 coluna no celular). Componente novo ui_kits/babel-os/RostoPixel.jsx: rosto em pixel art (SVG 16x16, image-rendering pixelated), pisca e "respira" sozinho em repouso, olhar muda de vez em quando, estado "pensando" com reticências animadas, boca troca de forma (fechada/meia/aberta/o) a partir da prop `letra` (vogal abre mais); role="img" com aria-label; timers limpos no unmount; JS para de piscar/olhar sozinho com prefers-reduced-motion (a CSS global já desliga toda animação/transição nesse modo). AssistenteScreen.jsx: rosto acima da conversa, resposta do Reino aparece com efeito de digitação letra a letra sincronizado com a boca do rosto (com reduced motion, mostra a resposta inteira de uma vez e só pulsa a boca por um instante), texto da bolha do Reino em aria-live="polite"; chip quebrado "Como faço network no Bate Papo?" trocado por "Como funciona o Match do Reino?" (a base do assistente responde). DashboardScreen.jsx: 3 blocos novos consumindo a Edge Function pública reino-apis — `noticias` (mesmo id salvo, agora com 5 manchetes reais e "Ver todas"), `musica` (mini player com 3 sugestões fixas, resultados do Deezer, um único `<audio>` que para ao fechar o bloco ou sair do Dashboard) e `assistente` (RostoPixel + campo de pergunta + última resposta + botão "Abrir X" quando a API aponta pra uma tela) — todos ocultáveis pelo ✕ e restauráveis por "Personalizar", encaixados na coluna central existente sem quebrar a grade. index.html: RostoPixel.jsx registrado antes de DashboardScreen.jsx e do restante das telas. Verificação: `node .claude/dominic/checar-jsx.js` limpo nos 4 arquivos (RostoPixel, DashboardScreen, NoticiasScreen, AssistenteScreen); Dominic avisou 1 cor fora dos tokens (#eafcff nos olhos do rosto) — corrigido para `var(--text-strong)`. `sh montar-site.sh` + servidor próprio na porta 8170 + Playwright headless (venv do scratchpad): screenshots 1440×900 e 390×844 de Notícias (manchetes reais do Google Notícias, "Mais lidas" numerada, grade, kicker em caixa alta) e do Dashboard com os 3 blocos novos visíveis (notícias reais, 3 faixas de samba do Deezer, rosto do assistente); sequência de 4 screenshots do rosto durante uma resposta real ("Como funciona o Mapa Reino?") mostrando a boca em pelo menos 3 formas diferentes enquanto o texto crescia letra a letra; teste de fechar o bloco Música (some da tela) e restaurar por Personalizar (volta com os mesmos dados, confirmado por locator.count()==1 e screenshot); teste extra com `prefers-reduced-motion: reduce` no Assistente mostrando a resposta inteira de uma vez, sem digitação. Console sem erros meus nas 6 rodadas de teste — só o 400 pré-existente do login demo tentando autenticar no Supabase Auth (já documentado em entradas anteriores deste log, não é desta tarefa). Não verifiquei: toque em celular físico (só emulação Playwright) e o som saindo de verdade das prévias do Deezer (só o estado play/pause e a troca de ícone, sem áudio real no ambiente headless). Screenshots em /tmp/claude-1000/-home-marcos-Downloads-Reino/9efb7a55-d641-4b2c-beab-bcc960c2f91d/scratchpad/shots-ourives/. |
| 2026-09-21 01:19 | concluído | 2026-09-21 01:26 | O3 — tela de login estilo uncensored.com. Referência capturada em 1440 e 390 (headless) e estudada: hero 100vh, barra fixa, letreiro neon, painel de vidro raio 28px/blur 24px, fileira de ícones redondos + botão circular, linha "New additions", definição de dicionário no canto. Criados `ui_kits/babel-os/PortalLogin.jsx` (window.PortalLogin, props onEntrar/recuperacao/avisoInicial, mesma lógica de conta do LoginScreen via window.ReinoContas: entrar, cadastrar c/ confirmar e-mail, recuperarSenha, trocarSenha; código de afiliado preservado; céu em canvas próprio — estrelas, globo em grade, piso holográfico — a 30 fps, pausa com aba oculta, quadro parado com prefers-reduced-motion), `portal-login.css` (tokens + hg-*, Exo 2 itálico importado do Google Fonts para a definição) e `portal-preview.html` (só renderiza o PortalLogin; aceita ?recuperacao=1 e ?aviso=). index.html: só a tag link do CSS e o script do JSX. NÃO mexi em App.jsx, LoginScreen.jsx, login.css, contas.js. Nenhum asset/texto/vídeo da referência. checar-jsx.js ok. Teste em cópia própria (scratchpad/site-portal, porta 8125, sem montar-site.sh): 1440×900 e 390×844 sem rolagem horizontal; senha errada → "E-mail ou senha não conferem." dentro do painel; pílula troca para cadastro; carregando (aria-busy + spinner) e "confirme seu e-mail" testados com ReinoContas simulado na página; ?recuperacao=1 mostra Nova senha/Repita e esconde a pílula; reduced-motion zera a névoa; foco de teclado visível. Console: só o 400 do Supabase Auth na tentativa de senha errada (esperado). Prints em /tmp/claude-1000/-home-marcos/c1871f01-31d4-4ea6-acd2-6b3e7dce64d4/scratchpad/prints-portal/. |
| 2026-09-21 01:10 | concluído | 2026-09-21 01:26 | N1 (entrar só com conta real), partindo do diff da seção N sem reverter nada. contas.js: removidos os dois ramos de demonstração (entrar/cadastrar agora dão erro 'banco não configurado'); sessão salva com demo:true, id 'local' ou sem token é descartada já na carga do script; fetch do Auth embrulhado (falha de rede e 5xx viram mensagem em pt-BR); entrar exige access_token + user.id e só devolve conta se carregarConta confirmar no banco; iniciar devolve aviso quando não há banco ou ele está fora do ar (neste caso mantém a sessão real salva para quando voltar). App.jsx: motivo de não ter entrado vai para erroInicial do LoginScreen (caixa vermelha) e a conta só é aceita com token. LoginScreen.jsx: prop erroInicial. app-standalone.html (export antigo publicado em /app-standalone.html): catch não chama mais entrarLocal, só mostra erro; entrada exige sessão com token. Testes Playwright + Chrome do sistema em servidor próprio na porta 8124 (pasta scratchpad/site-n1, sem montar-site.sh): (a) senha errada e e-mail inexistente → 'E-mail ou senha não conferem.', continua no login, sem sessão; (b) localStorage com {demo:true,id:'local',situacao:'admin'} → abre no login e a chave some; (d) sem url do banco → mensagem ao abrir e ao tentar entrar; (e) Auth derrubado na rede → 'Não foi possível falar com o banco do Reino agora…' ao abrir e ao entrar; (f) cadastro real teste-n1+1789964715639@exemplo.com criado pela tela → aviso de confirmação, login bloqueado com 'Confirme seu e-mail…' (não deu para testar login real de ponta a ponta); caminho feliz com Auth simulado entra, mostra aviso de aprovação e continua logado após recarregar. Console: só 400/401 esperados do Auth e um 401 do POST em perfis logo após cadastro que exige confirmação (anterior a esta tarefa). Pendente: reino-app.html (export gzipado antigo, publicado em /reino-app.html) ainda tem contas com modo demonstração e entrarLocal; CLAUDE.md e PerfilScreen.jsx:62 ainda falam em modo demonstração. |
| 2026-09-21 02:12 | concluído | 2026-09-21 02:26 | Q (vídeo de fundo no login, com o 0.mp4 PROVISÓRIO). Script `ui_kits/babel-os/assets/login/preparar-video.sh <entrada> [saída]`: tira áudio, recorta a marca d'água PopVid.AI (CORTE_TOPO=44 px, largura recalculada para manter a proporção, ALINHAR_X centro; 608×320 → 524×276), gera fundo-login.mp4 (H.264 CRF 26, faststart, 297 KB), fundo-login.webm (VP9 CRF 42, 172 KB) e fundo-login-final.jpg (último quadro, 36 KB). PortalLogin.jsx: objeto VIDEO_LOGIN no topo (src, final, cardEm 4.5, subidaDur 1.2, pularDur, esperaMax 2.5, foco/focoCelular); `<video muted playsInline autoPlay preload="auto">` sem loop e sem poster (o poster seria o último quadro aparecendo antes do vídeo); requestVideoFrameCallback + timeupdate + ended disparam a entrada; estados do card espera/entra/pronto; header, main e aside com inert + aria-hidden e autoFocus desligado até o card entrar, depois foca o primeiro campo; pointerdown/keydown antes do cardEm pulam (vídeo vai ao fim e pausa; se o servidor não aceitar Range e o fim não for pesquisável, troca pelo JPG do último quadro); prefers-reduced-motion, ?recuperacao, erro e aviso → JPG desfocado e card direto; erro do vídeo/source, autoplay recusado ou 2,5 s sem tocar → céu em canvas + névoa + letreiro CSS (reserva). Com vídeo, o letreiro REINO vira só para leitor de tela e a marca do topo fica escondida. portal-login.css: .hg-portal-video (object-fit cover, object-position por variável, filtro blur 0→14px com transição), is-espera/is-entra/is-desfocado/tem-video, celular com focoCelular, reduced-motion sem transições. montar-site.sh: não publica o .sh. Teste: cópia em scratchpad/site-video, servidor próprio com Range na 8126 (o http.server do Python não aceita Range e deixava o vídeo não pesquisável — foi o que revelou o caso do salto), Playwright + Chrome do sistema, 1440×900 e 390×844: card entra em 4,50–4,57 s, meio da subida a +170 ms (translateY 168 px, opacidade 0,65), final parado em 5,88 s com blur 14px; foco programático no input escondido não pega; clique a ~1 s → vídeo em 5,83 s pausado e card; senha errada → "E-mail ou senha não conferem."; vídeo abortado e vídeo que nunca responde → canvas; reduced-motion, ?recuperacao=1 e ?aviso= → JPG desfocado e card direto. Console: 400 do Auth (senha errada), favicon 404, ERR_FAILED dos abortos forçados. Q1 NÃO marcado. Prints em /tmp/claude-1000/-home-marcos/c1871f01-31d4-4ea6-acd2-6b3e7dce64d4/scratchpad/shots-video/. Sem commit. |
| 2026-09-21 02:38 | concluído | 2026-09-21 02:50 | R2 e estrutura do R1 (pedido: "quero que seja todos" e "só apareça o card de login quando clicar a tela"). PortalLogin.jsx: VIDEO_LOGIN virou { lista: [{webm, mp4}], final, subidaDur 1.2, diretoDur 0.45, esperaMax 2.5, foco, focoCelular } (saiu cardEm, pularDur, requestVideoFrameCallback e o salto para o fim). Formato escolhido uma vez por canPlayType (webm VP9 ou mp4) e posto em src direto. Um item: um <video> com autoplay+loop. Dois ou mais: dois <video> empilhados (is-frente/is-atras); no ended o de trás, já carregado, vem para a frente e o que saiu recebe o item seguinte (se o de trás não estiver pronto, recomeça o atual). Capa: <button class="hg-portal-capa"> em tela cheia (z-index 6) enquanto o card espera; abre no pointerdown e só some no click (ou 1,5 s depois do pointerup, ou no pointercancel), então o clique nunca cai no card nem na pílula. No toque não foca o campo (o teclado virtual cobriria a subida); no mouse e no teclado foca o e-mail. Teclado: Enter, Espaço e Tab abrem (keydown em captura com preventDefault). Dica "Clique na tela para entrar" / "Toque na tela para entrar" (pointer: coarse), pílula de vidro com pulso de opacidade e neon ciano, some ao abrir. Card continua inerte + aria-hidden até entrar. Reduced-motion, recuperação, erro/aviso inicial e vídeo que falha → card direto, sem capa. portal-login.css: .is-frente/.is-atras, .hg-portal-capa, .hg-portal-dica, @keyframes hg-portal-pulso, pulso desligado no reduced-motion. preparar-video.sh: aceita N arquivos, pula repetidos pelo sha256, 1 único → fundo-login.*, 2+ → fundo-login-1.*, -2.*..., JPG final do 1º, apaga fundo-login* antigos da saída e imprime a lista para colar; SAIDA=pasta opcional. Rodado nos 7 arquivos da pasta → "7 arquivo(s), 1 diferente(s)", mp4 byte a byte igual ao publicado (não regravei os assets). Teste Playwright (Chrome do sistema, servidor Node com Range na 8126): ver linha do LOG.md; a lista de 2 foi testada numa cópia (scratchpad/site-lista2) com um 2º vídeo espelhado gerado pelo script. Prints em /tmp/claude-1000/-home-marcos/c1871f01-31d4-4ea6-acd2-6b3e7dce64d4/scratchpad/shots-r2/. R1 continua aberto. Sem commit. |
| 2026-09-21 06:07 | concluído | 2026-09-21 06:53 | W — login imersivo, só no local (sem commit/push/deploy, sem tocar produção). Fundo: 'Reino Animado.html' é um bundle autoextraível; extraí a cena (reino-video.jsx, 20 s em loop) e as duas imagens e portei para app/componentes/FundoReino.jsx com relógio próprio (30 qps, pausa com aba escondida, quadro parado com movimento reduzido); cobre em paisagem; em retrato encaixa o letreiro e preenche com a própria cena desfocada (FUNDO_REINO.retrato, decisão para o fundador). Reserva: erro na cena → iframe do original copiado para assets/login/reino-animado.html; imagem que não carrega → vídeo antigo. Tela app/telas/LoginImersivo.jsx + app/estilos/login-imersivo.css: bolhas 'Reino' + barra com anel de energia girando (ciano/magenta), olho, voltar etapa, Enter envia, Esc volta, foco sempre na barra (useLayoutEffect). Cadastro: nome, empresa, CNPJ (máscara + DV), foto (câmera ao vivo no PC / capture no celular / galeria, recorte quadrado com zoom, 512 px webp ou jpeg), e-mail, usuário (RPC usuario_disponivel ao digitar), senha + confirmação, resumo com editar e código de afiliado (registrarCadastro continua). Login: usuário→senha via função reino-login (aceita e-mail). Validar: e-mail mascarado, reenviar, Gmail/Yahoo/Outlook com seta em nova aba, 'Já validei'. Backend: migração 2026-09-21_login_imersivo_usuario_empresa_cnpj.sql (usuario/empresa/cnpj, criar_perfil, proteger_perfil sem troca de usuario/cnpj, RPCs, tentativas_login, bucket avatares), functions reino-cadastro, reino-login, _shared/reino-validar.ts; config.toml local com confirmação de e-mail e verify_jwt=false das duas funções. Achado: Dashboard/Notícias/Música/Assistente/Academy/globo têm a URL de produção fixa no código (não usam window.REINO_SUPABASE) — o teste local troca na cópia; corrigir no app fica como pendência. Evidência: e2e-login-imersivo-local.cjs 109/109 duas vezes; prints /tmp/claude-1000/reino-login-prints. Supabase local parado. |
| 2026-09-21 08:22 | concluído | 2026-09-21 09:03 | X1 — "deixe mais rápido o vídeo de fundo, para parecer fluido". O fundo não era vídeo: era a cena React/SVG do FundoReino.jsx redesenhada a cada quadro (feTurbulence + feDisplacementMap na água e nas quedas, ~70 divs animados). Medido antes de mexer, no notebook do fundador, Chrome com GPU, 1440×900, 6 s de requestAnimationFrame: **20,1 quadros/s, 23 quadros acima de 50 ms, mediana 50 ms, pior 83 ms** — e em medições seguintes, com a página assentada, cai para **12–13 quadros/s com 60–64 quadros longos e pior caso 150–166 ms**. Solução: pré-renderizar a cena e tocar o vídeo. (1) `scripts/render-fundo/index.html` (fora do site — montar-site.sh só copia app/) carrega o MESMO app/componentes/FundoReino.jsx com `window.__REINO_RENDER_FUNDO = true` definido antes; só nesse caso o arquivo expõe a cena, e a página instala `window.__fixarT(t)`, que fixa o tempo e só devolve depois do quadro pintado. No app publicado esse "se" é sempre falso e nenhum gancho fica ativo. (2) `scripts/renderizar-fundo.cjs` (Playwright do npx, canal chrome do sistema) sobe um servidor na raiz do projeto, abre a página e tira 420 PNGs 1920×1080 (14 s a 30 q/s) em ~11 min, fora do repositório (CLAUDE_JOB_DIR/tmp/reino-fundo). Laço conferido comparando o quadro TOTAL com o 0 e o TOTAL+1/30 com o 1: PSNR 64,2 dB e 60,4 dB — sem salto (a matemática da cena já era periódica em TOTAL depois da correção do ciclo de 14 s). (3) ffmpeg: WebM VP9 crf 40 (4,84 MB), MP4 H.264 crf 28 high/4.0 yuv420p +faststart sem áudio (4,00 MB), poster webp do primeiro quadro (0,14 MB) — 8,98 MB somados, assets/login vai a 15 MB e app/ a 22 MB. CRF escolhido medindo: 34/25 davam 7,97 + 6,20 MB sem ganho visível; contra o banding do céu entra `noise=alls=2:allf=t+u` antes do yuv420p. Qualidade final: PSNR-Y 37,56 dB (webm) e 37,75 dB (mp4), recortes do céu e do letreiro conferidos por mim, sem faixas. (4) FundoReino.jsx: `<video autoplay muted loop playsinline preload="auto">` com poster dentro do MESMO palco 1920×1080, usando o `enquadrar()` de sempre (cobre em paisagem; encaixa em retrato sobre a cena desfocada, agora desfocando o poster), CSS novo `.hg-fundo-reino-video` em login-imersivo.css. Escada de reserva: vídeo → cena JS (erro do vídeo, autoplay recusado ou 8 s sem começar, sem contar o tempo com a aba escondida) → iframe do original → onFalha (vídeo antigo). Movimento reduzido: só o poster, sem autoplay. Aba escondida: pausa e volta a tocar. (5) `scripts/medir-fundo.cjs` (novo) mede fps, quadros longos e long tasks no Chrome com GPU; o modo é injetado por `window.FUNDO_REINO_MODO` antes do app carregar, então não existe gatilho por URL no site. **Depois: 60,1 / 59,6 / 60,1 quadros/s em 1440×900, 0 quadro acima de 50 ms, pior quadro 16,8 ms, 0 long task; 60,1 quadros/s em 390×844.** Ou seja, ~3× a 5× mais quadros por segundo e o fim dos quadros longos — é a diferença que o fundador pediu, com número. Vídeo começa a tocar 0,44 s depois do fundo montar (os ~10,7 s até lá são o Babel compilando o app no navegador, que já era assim). Testes: os três comportamentos conferidos com Playwright (movimento reduzido → vídeo pausado, autoplay off, poster; aba escondida → pausa, volta → toca; vídeo bloqueado na rede → cai sozinho na cena JS, sem iframe, sem faixa preta); prints 1440×900 e 390×844 abertos e conferidos por mim (cena nítida, letreiro aceso, sem preto, barra de login por cima); checar-jsx.js limpo; console só com o favicon 404 de sempre. CLAUDE.md atualizado (linhas de assets/login e scripts/). NÃO fiz: commit, push, deploy nem nada no Supabase — fica para a Vyra. Arquivos e prints em /tmp/claude-1000/reino-fundo. Pendência para a Vyra decidir: o peso. Os degraus de baixo da escada (reino-animado.html 1,99 MB + fundo-login.mp4/webm 3,16 MB) somam 5,15 MB que só existem como reserva; dá para enxugar se o repositório apertar. |
| 2026-09-21 08:22 | concluído | 2026-09-21 09:22 | Y — "melhore ao maximo o app de noticias" + "deixar responsivo a tela de noticias. trazer mais cara de blog tecnologico com as imagems que vem de do site". **Achado que mudou o plano**: o RSS do Google Notícias não traz foto, e a página `news.google.com/rss/articles/...` (577 KB) tem como `og:image` só o logo do Google — conferi em 8 matérias, todas com a mesma imagem; a URL real da matéria não está no HTML (é resolvida por JS) e o `batchexecute` respondeu erro sem a assinatura da página. Então troquei a fonte: `supabase/functions/reino-apis` agora lê o RSS **dos próprios veículos** (lista fixa no servidor, 14 feeds públicos: InfoMoney, EXAME, Brazil Journal, NeoFeed, Startups, Agência Brasil, g1 Economia, g1 Tecnologia, Canaltech, Tecnoblog, Olhar Digital, Mobile Time, TI Inside, AdNews) — de lá vêm link direto para o site, resumo curto e a foto da matéria (`media:content`, `enclosure`, `mediaurl`, `imagem-destaque` ou a primeira `<img>` do conteúdo; descarta logo/pixel/svg/rastreador). Cada busca tem AbortController de 4,5 s e leitura cortada em 360 KB; cache em memória de 5 min por feed (as abas dividem a mesma leitura) e, se o feed falhar agora, fica valendo o que já havia. Dedupe por link e por assinatura do título (8 palavras de peso + total), intercalação por veículo na ordem da lista (negócios e economia abrem a capa), 8 editorias (Destaques, Negócios, Economia, Tecnologia, Marketing, Crédito e juros, Empreendedorismo, Política e empresas) — as três mais estreitas são recorte por palavra sobre o pool e o Google Notícias completa quando rendem menos de 12. Rota nova `noticias-editorias` (lista de abas e veículos); `cidade`, `musica` e `assistente` intactas; CORS e verify_jwt como estavam. Dois bugs achados e corrigidos pelo caminho: entidades `&ecirc;`/`&ccedil;` apareciam cruas no resumo (agora viram letra + acento combinado e normalizam) e a limpeza de HTML vazava atributos (`alt`, `data-large-file`) quando a legenda tinha ">" dentro de aspas — foi visto no print, não no teste. Front: `app/servicos/noticias.js` novo (busca, cache stale-while-revalidate em localStorage com try/catch e limpeza por cota, dedupe, salvas, lidas, compartilhar, tempo relativo, tom/inicial da capa de reserva), `app/estilos/noticias.css` novo (classes `.hg-nt-*`, só tokens; responsivo por *container query*: 1 coluna <540, 2 <1180, 3 <1500, 4 acima; abas rolando na horizontal; alvos de 44 px; esqueleto; `prefers-reduced-motion`), `NoticiasScreen.jsx` reescrita (manchete grande com foto e título sobre degradê, cartões 16:9 com etiqueta/veículo/tempo/trecho, "Em alta agora", veículos da edição, busca, filtro por veículo, 3 ordenações, aba Salvas, marcação de lidas, atalhos "/" e "r", `role=status` com `aria-live`, links em nova aba com `rel="noopener noreferrer"`, imagens `lazy`+`decoding=async`+`referrerpolicy=no-referrer` com proporção fixa para o layout não pular) e o bloco de notícias do `DashboardScreen.jsx` passou a usar o mesmo serviço (manchete com foto + 4 miniaturas; nada mais do Dashboard foi tocado). CSS antigo `.hg-jornal-*` saiu do app-shell.css (ninguém mais usava). Evidência: `supabase/testes/e2e-noticias.cjs` novo, 50 conferências, todas passaram, com a função rodando local por `deno run` (porta 8153, cópia em /tmp, o arquivo do projeto não foi alterado para isso) — simulações por `page.route` (sucesso, lista vazia, erro 502, timeout, cache velho na tela + atualização por trás, salvar/lida/compartilhar, troca de editoria, busca, imagem que falha virando capa do Reino), dados reais (39 cartões, 37 fotos dos sites, 14 veículos, uma foto real de 1280 px desenhada) e leitura real da função em produção (200, 30 itens: o formato antigo continua passando pela limpeza do app). 1440×900, 768×1024 e 390×844 sem rolagem horizontal e sem erro de JavaScript. Prints conferidos um a um em /tmp/claude-1000/reino-noticias-prints (foi assim que achei o vazamento de HTML no resumo e o desalinhamento dos filtros no celular, os dois corrigidos). **Não fiz**: commit, push, `functions deploy` e qualquer escrita em produção — a única chamada à produção foi a leitura pública da rota atual. Não toquei em FundoReino.jsx nem nos assets do login. Não verifiquei: celular físico (só emulação) e o comportamento dos feeds ao longo do dia (testei numa janela de ~1 h). |
