/** Cápsula violeta de valor curto: compatibilidade de match, nível do mapa. */
export interface PillProps {
  as?: "b" | "span" | "div";
  className?: string;
  children?: React.ReactNode;
}
export declare function Pill(props: PillProps): JSX.Element;
