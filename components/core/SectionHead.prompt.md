Cabeçalho de painel. Já vem embutido em `Panel` — use solto apenas dentro de `<section className="hg-panel">` escrito à mão.

```jsx
<SectionHead title="Minha lista" subtitle="Toque num ativo para ver os detalhes" actions={<Pill>96%</Pill>} />
```
