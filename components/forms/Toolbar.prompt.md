Barra de ferramentas de painel (busca + filtros + ações).

```jsx
<Toolbar as="form" onSubmit={buscar}>
  <Input placeholder="Adicionar: PETR4, HGLG11…" />
  <Button variant="cyan" type="submit">Adicionar</Button>
  <Select>…</Select>
</Toolbar>
```
