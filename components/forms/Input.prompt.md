Campo de texto padrão.

```jsx
<Input id="novo-ticker" placeholder="Adicionar: PETR4, HGLG11…" autoComplete="off" />
```

Placeholders são exemplos reais, não instruções. Campo de link de afiliado usa `readOnly` e fonte mono.
