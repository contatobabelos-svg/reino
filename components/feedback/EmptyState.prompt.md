Estado vazio e estado de erro — o produto usa o mesmo bloco para os dois.

```jsx
<EmptyState icon="alerta" title="Não foi possível carregar as cotações" description="Defina HGBRASIL_KEY no servidor e reinicie-o." />
```

O ícone pulsa sozinho (`hg-breathe`). Texto sempre explica a causa e o próximo passo.
