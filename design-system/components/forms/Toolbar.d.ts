/** Linha de controles que quebra em várias linhas; o Input interno cresce até 360px. */
export interface ToolbarProps {
  as?: "div" | "form";
  className?: string;
  children?: React.ReactNode;
}
export declare function Toolbar(props: ToolbarProps): JSX.Element;
