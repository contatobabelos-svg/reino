Chave de preferência — usada só na tela de Configurações, uma por linha, separadas por filete.

```jsx
<Switch id="demo" label="Dados de demonstração" hint="Desligue para exigir uma API configurada." defaultChecked />
```
